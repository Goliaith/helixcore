"""HelixCore version information."""

__version__ = "0.3.0"  # Serena Sunset complete + full local stack (LocalCodeIntel + LocalProjectMemory) as canonical; packaging validation + dist polish + external usability hardening (safety shim, configure, standalone reports)